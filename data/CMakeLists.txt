if (ENABLE_XFCE)
    install(FILES ${CMAKE_CURRENT_BINARY_DIR}/sntray.desktop DESTINATION share/xfce4/panel/plugins)
endif()
if (ENABLE_VALAPANEL)
    include(GSettings)
    add_schema("org.valapanel.toplevel.sntray.gschema.xml")
    install(FILES ${CMAKE_CURRENT_BINARY_DIR}/sntray.plugin DESTINATION lib/vala-panel/applets)
endif()
